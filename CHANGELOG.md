# Changelog

## 0.6.1 — 2026-08-24

Волна 1 миграции на per-instance состояние (пакет A): драйв-трио и goal-executor. Внешний API не меняется.

- `social-drive`, `cognitive-hunger`, `creative-drive` переведены на фабрики (`createSocialDrive`, `createCognitiveHunger`, `createCreativeDrive`): DriveEngine и логгер инкапсулированы в инстансе; init-обёртки используют `dispose()` (тихая остановка при повторной инициализации), stop-обёртки — `stop()` (с логом)
- `goal-executor` переведён на фабрику `createGoalExecutor()`: счётчик целей живёт в замыкании инстанса
- В каждом модуле остался ровно один module-level `let` — слот активного инстанса для совместимости init/stop; слоты уйдут с переходом на Cordis-скоупы
- Семантика goal-executor после stop: статистика обнуляется вместе с инстансом (раньше счётчик жил до следующего init)
- +3 теста (новый файл goal-executor.test.ts + изоляция инстансов драйвов в drives.test.ts)
- 710 тестов / 41 файл; tsc чисто, smoke OK

## 0.6.0 — 2026-08-24

Волна 0 миграции на per-instance состояние: pilot-модуль отработан, шаблон зафиксирован. Внешний API и поведение не меняются.

- `injection-metrics` переведён с 8 module-level `let` на фабрику `createInjectionMetrics()`: состояние изолировано в замыкании инстанса
- Экспортирован инстанс по умолчанию `defaultInjectionMetrics`; свободные функции (`recordInjectionCycle`, `getInjectionMetrics`, `resetInjectionMetrics`) стали тонкими обёртками — все существующие места вызова не тронуты
- Шаблон для следующих волн: фабрика + инстанс по умолчанию + обёртки; дальше инстансы уйдут в Cordis-скоупы
- +2 теста: фабрика создаёт независимые инстансы; обёртки пишут только в инстанс по умолчанию
- 707 тестов / 40 файлов; tsc чисто, smoke OK

## 0.5.2 — 2026-08-24

Рефакторинг по итогам ревью v0.5.1: фрейминг и его детекция из одного источника, снята инверсия слоёв. Поведение не меняется.

- Новый `src/modules/autonomy-markers.ts`: тег, префикс тега, строки фрейминга доставки (`AUTONOMOUS_FRAMING_LINES`) и `isAutonomousInput()` живут в одном файле слоя modules
- Префикс детекции фрейминга выводится из первой строки `AUTONOMOUS_FRAMING_LINES`, а не дублируется литералом: deliverer собирает текст доставки из того же массива — переформулировка фрейминга не может тихо сломать детекцию
- `plugin/config.ts` оставляет только реэкспорты маркеров (существующие импорты `vital-impulse`, `cycles`, `context` не тронуты)
- `message-guard` импортирует префикс из `./autonomy-markers.ts` (свой слой) вместо `../plugin/config.ts` — граница «модули не зависят от plugin» восстановлена
- +2 теста против дрейфа (текст deliverer'а проходит детекцию и guard; префикс = первая строка фрейминга)
- 705 тестов / 40 файлов; tsc чисто, smoke OK

## 0.5.1 — 2026-08-24

Единая детекция автономного ввода — фикс дефекта, найденного eval-харнессом v0.5.0.

- Дефект: проактивная доставка приходит с фреймингом («Это не сообщение пользователя…») перед тегом `<autonomous-intent>`, и все проверки `startsWith("<autonomous-intent>")` (гейт инструментов, pre-step, endCycle, наблюдатель) на доставленных сообщениях не срабатывали; в smoke «loop-breaker» фактически прикрывал минимальный интервал доставки
- `config.ts`: `AUTONOMOUS_FRAME_PREFIX` + `isAutonomousInput()` — распознаёт ровный тег, тег с атрибутами и фрейминг
- Все четыре точки детекции (наблюдатель, гейт инструментов, pre-step, endCycle) переведены на единый помощник
- `message-guard`: фрейминг-доставка фильтруется как служебное сообщение — доставленный промпт больше не обрабатывается контуром обучения как реплика пользователя
- `context.ts`: тема автономного исследования извлекается после тега, а не из строк фрейминга
- +2 сценария в `autonomy.eval.ts` (гейт на фрейминг-доставке; endCycle + loop-breaker на фрейминг-цикле), новый `autonomous-detection.test.ts` (7 тестов)
- 703 теста / 40 файлов; loop-breaker теперь срабатывает по-настоящему

## 0.5.0 — 2026-08-24

Eval-харнесс на полный пайплайн — страховка для будущих рефакторингов.

- `src/eval/harness.ts`: запуск плагина целиком на мокнутом dsh-контексте (изолированный dataDir, честный cleanup эффектов)
- `pipeline.eval.ts`: golden-сценарий «живой» диалог → цикл обучения → pre-step → /brain status
- `learning-loop.eval.ts`: связка источник награды → reward-ledger → strategy-bandit на реальных событиях шины, включая мульти-точковую атрибуцию
- `autonomy.eval.ts`: проактивная доставка с фреймингом, гейт web-инструментов в автономных циклах, loop-breaker (проверен точной строкой лога, а не молчанием)
- `recall-regression.eval.ts`: «при таком состоянии памяти recall возвращает это» + донос памяти до контекста модели
- 18 интеграционных сценариев (694 теста всего, 39 файлов)

## 0.4.1 — 2026-08-24

Дочистка по итогам независимого ревью v0.4.0.

- `strategy-bandit`: слот атрибуции награды теперь свой для каждой точки решения — награда уходит самому позднему выбору в окне атрибуции; вторая точка решения не сломает мульти-точковую атрибуцию
- `dmn`: дедуп мыслей о пробелах по точной фразе «Knowledge gap in "<тема>"» вместо substring-сравнения — короткие темы («cat») больше не сталкиваются с длинными («catalog»)
- README: «единственная часть, которая учится» → «единственная явная петля обучения с политикой» (остальные модули статистически адаптируются без политики)
- 3 новых теста (676 всего)
## 0.4.0 — 2026-08-24

Честность и петля обучения (RL-lite): без железа и без обучения весов.

- README: секция «Что под капотом на самом деле» — конвейер эвристических фильтров, статистические хранилища, инъекции контекста; имена модулей честно названы метафорой для читаемости
- Слияние дублирующихся драйвов: `curiosity-drive` — единственный канонический источник пробелов в знаниях; `cognitive-hunger` получает темы DMN из его пробелов (`topicProvider` в DriveEngine) вместо общей запасной темы; DMN потребляет пробелы (во сне) и дедуплицирует мысли о них (окно 24 ч) — меньше шума в инъекциях
- Ступень 0 — `reward-ledger`: единая функция награды (взвешенные вклады дофамина, реакций пользователя, подкреплений, валидаций мозжечка и прогнозов) и журнал наград `.brainagent/reward/ledger.json`
- Ступень 1 — `strategy-bandit`: UCB1-бандит по накопленной награде выбирает объём контекста ("lean" / "standard"); политика хранится в таблицах, а не в весах — настоящее RL без GPU. Конфиг `learningLoop`
- Новые события шины: `reward:recorded`, `bandit:arm-chosen`; 19 новых тестов
## 0.3.0 — 2026-08-24

Рефакторинг: монолитный `apply()` разбит на слои, поведение не менялось. Все фиксы 0.2.1 и 0.2.2 сохранены.

- `src/index.ts` сокращён с 2185 до ~770 строк — остался только «композитор»: инициализация модулей, регистрация хуков dsh и cleanup
- Новая структура `src/plugin/`: `config.ts` (схема и merge конфига через общий цикл с картой переименований вместо ручного 50-строчного маппинга), `autonomy.ts` (проактивная доставка, резолвер намерений, автономные эмоции; служебные переменные объединены в `AutonomyState`), `cycles.ts` (жизненный цикл startCycle/endCycle, единый helper `participatingModules` вместо 3 копий), `context.ts` (обработчик `agent/pre-step` — recall и сборка инъекций)
- `tsconfig.json` стал самодостаточным standalone-проектом: убраны наследование `../tsconfig.base.json` и ссылки на `../vendor/...`, которых нет вне monorepo (раньше `pnpm typecheck` и vitest падали с «Tsconfig not found»)
- Починен `.smoke-phase2.mjs` (в mock не хватало сервисов `commands`/`agents`)
- Пересобран `lib/index.js` — бандл теперь соответствует исходникам
- Проверка: `tsc --noEmit` чисто, 654/654 тестов, smoke-phase2 и smoke-phase3 проходят

## 0.2.2 — 2026-08-24

Хотфикс по следам живой проверки в dsh: два последних источника мусора в инъекциях.

- `mastery-drive`: домен из события prediction-error теперь извлекается как первый токен строки (`domain/complexity: …` → `domain`) — раньше строка целиком ("creative/extreme: worse than expected") становилась «доменом», порождала фальшивые satiety-состояния и мусорные «пробелы в знаниях» в фоновых мыслях
- `prefrontal-cortex`: блок "Learned Workflow Available" больше не вливается, если у лучшего сценария нет шагов — пустой workflow модели бесполезен
- Сопровождено ручной чисткой персистного состояния (застрявший инсайт «110% error rate», recurringIssues, отравленный domainPerformance, псевдодомены mastery-drive)

## 0.2.1 — 2026-08-24

Хотфикс четырёх багов, найденных при разборе реальной инъекции `<brainagent-context>`.

- Новый модуль `message-guard`: наблюдатель `session/event` больше не считает собственные инъекции плагина (`<brainagent-context>`, `<autonomous-intent>`) репликами пользователя — раньше привычки, любопытство и эпизоды учились на собственном контексте (самоотравление)
- Брак-контроль триггеров в `procedural-extractor` (`isQualityTrigger`): обрезанные фрагменты вида "any files (" и "create ANY ." больше не сохраняются как выученные сценарии и не предлагаются агенту
- Потолок error rate 100% в `learning-coordinator` (`Math.min(1, …)` во всех трёх местах расчёта) — раньше диагностика показывала 110%
- Устойчивое определение языка: `mirror-neurons` не определяет язык на выборках короче 10 букв; `cerebellum` не считает mismatch'ем короткие ответы (<40 букв) и смешанный двуязычный текст (доля кириллицы 35–65%)
- 13 новых тестов (`v021-fixes`): всего 33 файла, 654 теста

## 0.2.0 — 2026-08-24

Интеллект: семантический поиск на эмбеддингах, двуязычные эвристики, обучение на «не зашло».

- Новый модуль `proactive-feedback`: обучение на реакциях пользователя на автономные (проактивные) сообщения. Отвержение («хватит», «не надо», "stop it") копит score подавления темы; при достижении порога домен подавляется на кулдаун — автономные намерения этой темы отбрасываются, а подсказка в проактивном фрейме просит её не заводить. Негатив считается с половинным весом, позитив снижает score. Score затухает со временем (0.25/день). До v0.2.0 реакции сохранялись «neutral» с важностью 0 — агент ничему не учился
- Новый общий банк эвристик RU/EN `i18n-heuristics`: единая классификация обратной связи (positive / negative / rejection) с приоритетом rejection → negative → positive. Используется proactive-feedback и basal-ganglia (детекция подкрепления). Исправлены границы кириллических слов: JS `\b` не работает с кириллицей — паттерны переведены на явные lookaround-границы
- Эмбеддинги в hippocampus: полноценная ленивая активация — если провайдер эмбеддингов появился в конфиге после старта, они включаются без перезапуска (каталог, загрузка кэша, бэкфилл). Ollama по умолчанию http://localhost:11434 (бесплатные локальные эмбеддинги). Защита от недоступной Ollama: после первой неудачи запросы не повторяются до смены конфига. Кэш эмбеддингов пишется атомарно (tmp + rename)
- Диагностика `/brainagent neuro`: новые секции — Proactive Feedback (вкл/выкл, счётчики реакций, подавленные домены) и Memory Search (бэкенд поиска TF-IDF/эмбеддинги, провайдер, модель, число кэшированных векторов по слоям памяти)
- Новый конфиг `proactiveFeedback` (suppressionThreshold, rejectionStep, negativeStep, positiveStep, decayPerDay, cooldownMs, maxTrackedDomains) и флаг `modules.proactiveFeedback` (включён по умолчанию)
- 19 новых тестов (i18n-heuristics, proactive-feedback): всего 32 файла, 641 тест
## 0.1.2 — 2026-08-24

Укрепление ядра автономии: консолидация драйвов, надёжность персистентности, рабочие флаги конфигурации.

- Единый DriveEngine (`src/modules/drive-engine.ts`): модули Social Drive, Cognitive Hunger и Creative Drive переведены на общий движок с тонкими обёртками — затухание (циркадное + серотониновое), уровни потребности, сигналы (желание → смещение DMN → позыв) и эскалация Vital Impulse работают одинаково во всех трёх
- Доменный фильтр дофаминовых наград уже существовал во всех четырёх драйвах (включая Mastery); в этом релизе он централизован в DriveEngine — исключено расхождение реализаций
- Drive Arbiter: гард реентрантности — события, эмитимые во время арбитража, больше не вызывают рекурсивный повторный арбитраж
- Персистентность горячих путей (все драйвы, Mastery, Vital Impulse, дофаминовая система): запись с debounce (500 мс) + атомарно (tmp + rename) — state.json больше не повреждается при резком завершении процесса
- Флаги конфигурации `modules.metabolicBudget`, `modules.emergentModules`, `modules.interoception` теперь реально работают: модули инициализируются только при включённом флаге
- Новая настройка `contextInjection.maxChars` (по умолчанию 12000) и метрики prompt-инъекций: число циклов, превышения бюджета, средний объём вливаний — выводятся в `/brainagent neuro`
- Файлы состояния версии 0.1.1 читаются новым движком автоматически (поддержка legacy-ключей)
- 21 новый тест (drive-engine, persist, injection-metrics, arbiter-reentrancy): всего 30 файлов, 622 теста

## 0.1.1 — 2026-08-23

Исправления и тестирование ядра автономии и памяти (по итогам независимого код-ревью).

- Vital Impulse: `refractoryPeriodMs` теперь реально соблюдается (кулдаун 30 с после срабатывания), статистика честно сообщает состояние рефрактерности
- `forceImpulse` больше не обходит анти-спам (хабитуация, GABA, счётчик подряд идущих срабатываний)
- Vital Impulse и драйвы (social, cognitive, creative) корректно сбрасывают счётчики при повторной инициализации
- Hippocampus: у Tier-2 «отложенных противоречий» теперь заполняется id нового факта — ветка dream-mode в `consolidate()` заработала
- LLM-вызовы обёрнуты таймаутом 60 с (AbortController) — зависший провайдер больше не остановит цикл агента
- Комментарий ai-embeddings соответствует реально поддерживаемым провайдерам эмбеддингов
- 30 новых тестов: vital-impulse (9), драйвы (15), hippocampus (6) — всего 601 тест
## 0.1.0 — 2026-08-22

Первый публичный релиз BrainAgent как плагина DeepSeek Harness.

- Когнитивная архитектура из ~60 модулей
- Память: эпизодическая, семантическая, процедурная
- Эмоции и эмпатия: Amygdala, Mirror Neurons, Emotional Memory
- Обучение: дофаминовая система, базальные ганглии, Learning Coordinator
- Автономия: 4 драйва, Vital Impulse, Goal Stack/Executor, DMN, Curiosity Drive
- Саморегуляция: циркадный ритм, Dream Mode, Attention Gate, Token Economy
- Команды `/brain` в чате
- 571 unit-тест
- Лицензия: BrainAgent Noncommercial License 1.0
