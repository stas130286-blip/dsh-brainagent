# Dependencies & build artifacts
node_modules/
lib/types/
*.tsbuildinfo
# NOTE: lib/index.js (the shipped bundle) is committed on purpose —
# git installs must work without running any build scripts.

# Local runtime state (agent memory, profiles, sessions)
.dsh-home/
.brainagent/

# Local dev overlays (machine-specific paths)
cordis.yml

# Logs & temp
*.log
*.tmp
.DS_Store
*.tgz
